package com.scribe.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Aurora Background Orbs ────────────────────────────────────────────────────
val ScribeAuroraBase    = Color(0xFF0A0F2E)   // Deep navy base layer
val ScribeAuroraCobalt  = Color(0xFF1E40AF)   // Cobalt orb — left center
val ScribeAuroraPurple  = Color(0xFF6D28D9)   // Purple orb — right center
val ScribeAuroraTeal    = Color(0xFF0D9488)   // Teal orb — bottom

// ── Glass Panel Fills ─────────────────────────────────────────────────────────
val ScribeGlassFillHigh = Color(0x30FFFFFF)   // 19% white — main cards
val ScribeGlassFillMid  = Color(0x1EFFFFFF)   // 12% white — search / lighter panels
val ScribeGlassFillLow  = Color(0x12FFFFFF)   // 7%  white — very subtle overlay

// ── Glass Borders (specular highlight system) ─────────────────────────────────
val ScribeGlassBorderTop    = Color(0x8AFFFFFF)  // 54% — top specular edge
val ScribeGlassBorderBottom = Color(0x14FFFFFF)  // 8%  — faded bottom edge

// ── Glass Text ────────────────────────────────────────────────────────────────
val ScribeGlassTextPrimary   = Color(0xFFFFFFFF)  // Pure white
val ScribeGlassTextSecondary = Color(0xA6FFFFFF)  // 65% white
val ScribeGlassTextTertiary  = Color(0x66FFFFFF)  // 40% white

// ── Glass Accents ─────────────────────────────────────────────────────────────
val ScribeGlassCobalt  = Color(0xFF60A5FA)    // Electric cobalt on dark
val ScribeGlassEmerald = Color(0xFF34D399)    // Mint emerald on dark
val ScribeGlassRose    = Color(0xFFFC6C6C)    // Soft rose on dark
val ScribeGlassAmber   = Color(0xFFFBBF24)    // Amber on dark

// ── Glass Dividers ────────────────────────────────────────────────────────────
val ScribeGlassDivider = Color(0x1FFFFFFF)    // 12% white hairline
